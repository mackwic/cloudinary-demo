'use strict';

var module = angular.module('practiceApp');

var UploadCtrl = function($scope) {
    /*
        var ctrl = function($scope) {
            $scope.files = [];

            $scope.submitMsg = 'Choose which image to send';
        },

        var setFiles = function(elt) {
                    $scope.$apply(function() {
                    $scope.files = elt.files;
                };
            }
    };
    */
};

module.controller('UploadCtrl', UploadCtrl.ctrl);

